* If your system uses utf8 or utf16 by default then phpCMS has some trouble with it
  We know that Suse 9.1 uses utf8 by default so on that system you have to convert your files to utf8 before you can 
  use them. We are not able to fix this because utf8 is not enable by default in PHP 4.
